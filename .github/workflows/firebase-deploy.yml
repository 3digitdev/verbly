name: Build and Deploy
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Setup Elm environment
        uses: JorelAli/setup-elm@v1
      - name: Build
        run: elm make src/elm/practice/TimeAttack.elm --optimize --output=public/timeAttack.js
      - name: Archive Production Artifact
        uses: actions/upload-artifact@master
        with:
          name: dist
          path: dist
  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@master
      - name: Download Artifact
        uses: actions/download-artifact@master
        with:
          name: dist
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy
        env:
          FIREBASE_TOKEN: 1//0fXkHy4W3oliKCgYIARAAGA8SNwF-L9IrvqgEQPwIQFjSUtu628pip6tzAWWMfmUyQemOjL3JAS2q3uLeKasbXwadWkgPl8LB1Fc
